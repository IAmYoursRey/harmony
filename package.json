{
  "name": "harmony-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/web",
    "apps/server"
  ],
  "scripts": {
    "dev": "concurrently --kill-others --kill-others-on-fail -n \"SERVER,WEB\" -c \"cyan,green\" \"npm run dev -w apps/server\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w apps/web",
    "typecheck": "npm run typecheck -w apps/web",
    "test": "echo 'No tests yet'"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}