{
  "name": "geosense-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently --kill-others --kill-others-on-fail -n \"BACKEND,FRONTEND\" -c \"cyan,green\" \"cd backend && npx nodemon server.js\" \"cd frontend && npx vite\"",
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
