{
  "name": "geosense-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently --kill-others --kill-others-on-fail -n \"BACKEND,FRONTEND\" -c \"cyan,green\" \"cd backend && node --watch-path=. server.js\" \"cd frontend && node ../node_modules/vite/bin/vite.js .\"",
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
